@model ProductViewModel
@{
    ViewData["Title"] = "Details";
}

<div class="mx-auto"
     style="max-width: 500px">
    <vc:Product model="Model"></vc:Product>
    <a href="#" onclick="window.history.back()"
        class="btn btn-info btn-block">GO BACK</a>
</div>